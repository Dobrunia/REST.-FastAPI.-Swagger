version: "3.8"

services:
  glossary-grpc:
    build: .
    container_name: glossary-grpc
    ports:
      - "50051:50051"
    volumes:
      - glossary-data:/app/data
    environment:
      - HOST=0.0.0.0
      - PORT=50051
    restart: unless-stopped

volumes:
  glossary-data:

